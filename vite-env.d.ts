// This line can cause errors in some environments if TypeScript struggles to resolve Vite's client types.
// Since the project doesn't use Vite-specific client features like `import.meta.env`,
// it's safe to comment this out to resolve potential build issues.
// /// <reference types="vite/client" />
